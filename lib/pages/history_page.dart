import 'package:flutter/material.dart';

class HistoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('History Tonton')),
      body: Center(child: Text('Belum ada histori tonton.')),
    );
  }
}